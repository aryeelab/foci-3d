# Conda Recipe

This directory contains the Conda recipe used to build `foci-3d` for
publication on `bioconda`/`conda-forge`.

Build locally from the repository root with:

```bash
conda activate foci-3d
conda build conda-recipe
```
